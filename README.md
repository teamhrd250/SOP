# STARCOM SOP Visual V3

Aplikasi Streamlit standalone untuk visualisasi SOP Sales sampai After Sales.

## Jalankan lokal
```bash
pip install -r requirements.txt
streamlit run app.py
```

## Deploy Streamlit Cloud
Upload `app.py` dan `requirements.txt` ke root repository GitHub, lalu pilih `app.py` sebagai Main file path.
